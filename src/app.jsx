import './styles/base.css';

export default function App() {
  return (
    <main className="app-shell">
      <h1>Startup React App</h1>
      <p>React components will replace the static HTML prototypes here.</p>
    </main>
  );
}
